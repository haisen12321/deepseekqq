version: "3.8"
services:
  deepseek-qq-bot:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    container_name: deepseek_qq_bot
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file:
      - ../.env
    volumes:
      - ../data:/app/data
